// Storage is used to save/retrieve processed data
import { Databases } from '../..';

export const StorageService = {
    storeData: (data: any) => {
        // console.log(Databases.output);
    }
};
